export default function ChatButton() {
  // Dummy chat button for UI only
  return (
    <button
      className="ml-2 px-3 py-1 bg-blue-100 text-blue-700 rounded hover:bg-blue-200 transition"
      type="button"
      title="Start chat (dummy)"
      onClick={() => alert("Chat feature coming soon!")}
    ></button>
    );
    }
    